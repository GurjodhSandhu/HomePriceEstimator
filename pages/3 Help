import streamlit as st

st.header("How to Estimate Price")
st.write("1. Select a province via a drop down menu.")
st.write("2. Select a city via a drop down menu.")

st.write("3. Input the number of bedrooms by incrementing/decrementing via + - buttons or by typing out value")
st.write("4. Input the number of  bathrooms by incrementing/decrementing via + - buttons or by typing out value")
st.write("5. Add further filters via Advance Filters Tab on Top")
st.write("6. Click calculate button to output price")

